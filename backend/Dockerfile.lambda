FROM public.ecr.aws/lambda/python:3.13

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR ${LAMBDA_TASK_ROOT}
COPY pyproject.toml ./
COPY app ./app
RUN pip install --no-cache-dir --target "${LAMBDA_TASK_ROOT}" ".[aws]"

CMD ["app.lambda_handler.handler"]
